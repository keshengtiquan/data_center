export enum USER_TYPE {
  SYSTEM_USER = 'system_user',
  PROJECT_ADMIN = 'project_admin',
  GENERAL_USER = 'general_user',
}

export enum Memu_Module {
  COMPANY = 'company',
  PROJECT = 'project',
}
export enum WorkPlaceType {
  SECTION = 'section',
  STATION = 'station',
}

// onlyoffice文件类型
export enum DocumentType {
  docx = 'word',
  xlsx = 'cell',
  pptx = 'slide',
}
